services:
  
  mock-oauth:
    extends:
      file: devops/docker/docker-compose-local.yml
      service: mock-oauth
  db:
    extends:
      file: devops/docker/docker-compose-local.yml
      service: db

  app:
    extends:
      file: devops/docker/docker-compose-local.yml
      service: app

volumes:
  pgdata:
