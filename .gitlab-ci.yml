build:
  image: 
    name: quay.io/buildah/stable:v1.29.0
 
  variables:
    DOCKERFILE_NAME: 'Dockerfile'
  stage: build
  script:
    - export STORAGE_DRIVER=vfs
    - buildah build -t csfeer:$CI_COMMIT_SHORT_SHA .
