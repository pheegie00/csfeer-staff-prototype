services:
  
  mock-oauth:
    extends:
      file: ./docker-compose-base.yml
      service: mock-oauth
  oauth-setup:
    extends:
      file: ./docker-compose-base.yml
      service: oauth-setup
  db:
    extends:
      file: ./docker-compose-base.yml
      service: db

  app:
    image: "${FULL_IMAGE_NAME}"
    extends:
      file: ./docker-compose-base.yml
      service: app

  e2e:
    extends:
      file: ./docker-compose-base.yml
      service: e2e

volumes:
  pgdata:
